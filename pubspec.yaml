name: dhananjay_indihood_submission
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  stacked: ^1.7.1+1
  get_it:
  provider: ^4.3.2+2
  http: ^0.13.3
  encrypt: ^4.1.0
  connectivity: ^3.0.3
  shimmer: ^2.0.0
  maps_launcher: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
